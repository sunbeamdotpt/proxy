apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: sunbeam
  namespace: gateway-conformance
spec:
  gatewayClassName: sunbeam
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
